apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: teto-fotos
  namespace: teto
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 5Gi
